[project]
name = "okonomi"
version = "0.1.0"
description = "Self-decrypting encrypted HTML app portal"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "cryptography>=42.0.0",
    "beautifulsoup4>=4.12.0",
]
